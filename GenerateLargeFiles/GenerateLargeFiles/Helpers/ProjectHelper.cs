using System;
using System.Collections.Generic;
using System.Text;

namespace GenerateLargeFiles.Helpers
{
    public static class ProjectHelper
    {
        // I felt it was important to have a Helpers folder with at least one static class in
        // Otherwise, this probably won't even compile!
    }
}
